- if obj.errors.any?
  #error-box.alert.alert-danger.text-left
    %h2= "#{pluralize(obj.errors.count, "error")} prohibited this task from being saved:"
    %ul
      - obj.errors.full_messages.each do |msg|
        %li= msg